import os
import sys
import subprocess
import datetime



#our codebase
print(os.getcwd())
subprocess.io._io.open('app.py')
now = datetime.datetime.now()
other = datetime.datetime(1994,10,10,17,59)

print(other)

def outer(a):

    def nested(b):

        def nestedception(c):
            d = a + b + c
            return d
        
        return nestedception(b)
    return nested(a)

#neted reualt

print(outer(1))

data = {
'names':['Aliyu', 'Yazeed'],
'Positions':['Data scientist','Web analysis']


    }
